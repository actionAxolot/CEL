<h1>Listing pastors</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Bio</th>
    <th>Phone</th>
    <th>Email</th>
    <th>Address one</th>
    <th>Address two</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pastors.each do |pastor| %>
  <tr>
    <td><%= pastor.name %></td>
    <td><%= pastor.bio %></td>
    <td><%= pastor.phone %></td>
    <td><%= pastor.email %></td>
    <td><%= pastor.address_one %></td>
    <td><%= pastor.address_two %></td>
    <td><%= link_to 'Show', pastor %></td>
    <td><%= link_to 'Edit', edit_pastor_path(pastor) %></td>
    <td><%= link_to 'Destroy', pastor, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pastor', new_pastor_path %>
